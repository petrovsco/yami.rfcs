# Releases

The release registry. Furniture, never an RFC.

One `##` section per release, the heading text being the name exactly as RFCs
spell it in their `release:` field.

No releases yet — yami is at doctrine stage, and rule R3 holds it to four manual
weeks before anything is built.
